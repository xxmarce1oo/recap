// arquivo: src/App.tsx

// Este arquivo não é mais usado diretamente nas rotas, 
// mas você pode mantê-lo se quiser usá-lo para outros provedores de contexto no futuro.
// Por enquanto, ele não precisa renderizar nada específico.
export default function App() {
  return (
    <div>
      App Container
    </div>
  )
}